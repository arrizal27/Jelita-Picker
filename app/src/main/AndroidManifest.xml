<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smkn4bdg.jelitapicker">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JelitaPicker">
        <activity android:name=".ui.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.WelcomePageActivity" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.login.DaftarBerhasilActivity" />
        <activity android:name=".ui.login.DaftarActivity" />
        <activity android:name=".ui.main.MainActivity" />
        <activity android:name=".ui.profile.ProfileActivity" />
        <activity android:name=".ui.nabung.NabungActivity" />
        <activity android:name=".ui.setor.SetorActivity" />
        <activity android:name=".request.RiwayatActivity" />
        <activity android:name=".ui.setor.SetorBerhasilActivity" />
        <!--        <activity android:name=".PengepulActivity" />-->
        <activity android:name=".ui.profile.EditProfileActivity" />
        <activity android:name=".ui.profile.EditPasswordActivity"></activity>
        <activity android:name=".ui.profile.EditSuccessActivity" />

        <!--                <provider-->
        <!--                    android:name="androidx.core.content.FileProvider"-->
        <!--                    android:authorities="net.smallacademy.android.fileprovider"-->
        <!--                    android:exported="false"-->
        <!--                    android:grantUriPermissions="true">-->
        <!--                        <meta-data-->
        <!--                            android:name="android.support.FILE_PROVIDER_PATHS"-->
        <!--                            android:resource="@xml/file_paths"></meta-data>-->
        <!--                </provider>-->
    </application>

</manifest>